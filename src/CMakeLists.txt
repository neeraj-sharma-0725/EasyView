set(PROJECT_CURRENT_HEADER_PATH "${PROJECT_CURRENT_HEADER_PATH}/include")
set(PROJECT_CURRENT_SOURCE_PATH "${PROJECT_CURRENT_SOURCE_PATH}/src")

list(APPEND MAIN_SOURCES MainWindow)

foreach(source IN LISTS MAIN_SOURCES)
  list(APPEND PROJECT_SOURCES ${PROJECT_CURRENT_HEADER_PATH}/${source}.h)
  list(APPEND PROJECT_SOURCES ${PROJECT_CURRENT_SOURCE_PATH}/${source}.cpp)
endforeach()

set(PROJECT_SOURCES "${PROJECT_SOURCES}" PARENT_SCOPE)
