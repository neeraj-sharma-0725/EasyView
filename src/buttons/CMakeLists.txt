set(PROJECT_CURRENT_HEADER_PATH "${PROJECT_CURRENT_HEADER_PATH}/buttons")
set(PROJECT_CURRENT_SOURCE_PATH "${PROJECT_CURRENT_SOURCE_PATH}/buttons")

list(APPEND BUTTON_SOURCES PushButton)

foreach(file IN LISTS BUTTON_SOURCES)
  list(APPEND PROJECT_SOURCES ${PROJECT_CURRENT_HEADER_PATH}/${file}.h)
  list(APPEND PROJECT_SOURCES ${PROJECT_CURRENT_SOURCE_PATH}/${file}.cpp)
endforeach()

set(PROJECT_SOURCES "${PROJECT_SOURCES}" PARENT_SCOPE)
